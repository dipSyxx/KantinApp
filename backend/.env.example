# Neon PostgreSQL - pooled connection (used at runtime)
DATABASE_URL="postgresql://user:password@host.neon.tech/kantinapp?sslmode=require"

# Neon PostgreSQL - direct connection (used for migrations)
DATABASE_URL_UNPOOLED="postgresql://user:password@host.neon.tech/kantinapp?sslmode=require"

# Auth.js secret (primary)
AUTH_SECRET="change-me-to-a-random-secret-at-least-32-characters-long"

# Temporary fallback for auth secret migration.
# Deprecated: remove once AUTH_SECRET is configured everywhere.
JWT_SECRET="change-me-to-a-random-secret-at-least-32-characters-long"

# App version (used in health endpoint)
APP_VERSION="0.1.0"
